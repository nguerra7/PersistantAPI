const Pool = require('pg').Pool;
const db = new Pool({
    database: 'gmdb',
    user: 'docker exec -it de45e834c8e1 bash',
    password: '123',
    host: 'localhost', 
    port: 5432
});

module.exports = db;